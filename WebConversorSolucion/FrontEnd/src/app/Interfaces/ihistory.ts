export interface History {
  fromCoin: string;
  fromAmount: number;
  toCoin: string;
  toAmount: number;
  date: Date; // o Date, dependiendo del manejo
  email: string;
  name: string;
  lastName: string;
}
